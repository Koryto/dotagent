# Findings Ledger

Use this file as the cumulative findings memory for the task.

Each finding should keep a stable id across rounds.

## Ledger

1. `finding_id`
   Title:
   Severity:
   First round:
   Current status: open/resolved/rejected/deferred
   Owner:
   Fix commit:
   Notes:
