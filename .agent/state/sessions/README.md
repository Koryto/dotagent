# Sessions

This directory holds live per-session state files.

Naming convention:

- `state_<session_id>.md`

One active top-level agent session should map to one session state file.

The live project no longer has a single global session state file.

Use `session_state_template.md` as the source template for creating new session files.
