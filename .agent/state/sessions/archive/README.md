# Archived Sessions

This directory holds archived session state files moved out of the live `sessions/` directory.

Archival and deletion are CLI responsibilities, not routine agent responsibilities.
