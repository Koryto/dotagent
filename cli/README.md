# dotagent CLI

Implementation package for the `dotagent` operator CLI.

This directory contains the production code for:

- project-local framework initialization
- safe framework updates
- framework health inspection
- playbook runtime initialization
- assistant adapter generation

Planning artifacts live under:

- `../cli-task/`

The CLI should preserve:

- markdown as contract
- filesystem as durable state
- CLI as procedural operator layer
